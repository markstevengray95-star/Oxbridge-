# Server-only AI interviewer configuration.
# Never expose this value through NEXT_PUBLIC_* variables or commit a real key.
OPENAI_API_KEY=

# Optional. Defaults to gpt-5.6-luna for lower-cost interview turns.
OPENAI_MODEL=gpt-5.6-luna
