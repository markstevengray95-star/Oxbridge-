"use client"

import Link from "next/link"
import { useEffect, useMemo, useState } from "react"
import { ArrowLeft, ArrowRight, Brain, CalendarClock, Clock3, Gauge, RotateCcw, Sparkles, TrendingUp } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { LIVE_REPLAY_KEY, ORAL_RETEST_KEY, type OralRetest, type ReasoningReplay } from "@/lib/nextgen-prep"
import { RETRY_MOMENTS_KEY, buildRetryMoment, spacedRetestDate, type RetryMoment } from "@/lib/interview-academy"

function readReplays(){try{const value=JSON.parse(localStorage.getItem(LIVE_REPLAY_KEY)||"[]");return Array.isArray(value)?value as ReasoningReplay[]:[]}catch{return[]}}
function branchLabel(value?:string){if(value==="challenge")return"Challenge";if(value==="repair")return"Repair";if(value==="transfer")return"Changed condition";if(value==="clarify")return"Clarification";return"Deepen"}
function turnTime(active:ReasoningReplay,index:number){const turn=active.turns[index];if(turn.createdAt){const base=active.turns.find(t=>t.createdAt)?.createdAt;if(base){const seconds=Math.max(0,Math.round((new Date(turn.createdAt).getTime()-new Date(base).getTime())/1000));return`${Math.floor(seconds/60)}:${String(seconds%60).padStart(2,"0")}`}}const approx=Math.round(active.durationSeconds*Math.max(0,index)/Math.max(1,active.turns.length-1));return`${Math.floor(approx/60)}:${String(approx%60).padStart(2,"0")}`}

export default function ReasoningReplayPage(){
 const[replays,setReplays]=useState<ReasoningReplay[]>([]),[activeId,setActiveId]=useState(""),[notice,setNotice]=useState("")
 useEffect(()=>{const loaded=readReplays();setReplays(loaded);setActiveId(loaded[0]?.id??"")},[])
 const active=useMemo(()=>replays.find(item=>item.id===activeId)??replays[0],[replays,activeId])
 function scheduleRetest(){if(!active)return;let old:OralRetest[]=[];try{const raw=JSON.parse(localStorage.getItem(ORAL_RETEST_KEY)||"[]");if(Array.isArray(raw))old=raw}catch{}const previous=old.filter(item=>item.sourceReplayId===active.id).length,severity=active.improvements.length>=3?"high":active.improvements.length?"medium":"light",dueAt=spacedRetestDate(previous,severity);const next:OralRetest={id:`oral-${active.id}-${Date.now()}`,course:active.course,focus:active.focus,sourceReplayId:active.id,dueAt};localStorage.setItem(ORAL_RETEST_KEY,JSON.stringify([next,...old].slice(0,50)));setNotice(`Delayed retest scheduled for ${new Date(dueAt).toLocaleDateString("en-GB")}.`)}
 function retryTurn(index:number){if(!active)return;const turn=active.turns[index];if(turn.role!=="candidate")return;const previous=[...active.turns.slice(0,index)].reverse().find(t=>t.role==="interviewer");const moment=buildRetryMoment({replayId:active.id,course:active.course,turnId:turn.id,question:previous?.text||"Rebuild the reasoning from this point.",answer:turn.text,feedback:turn.feedback||active.improvements[0]});let old:RetryMoment[]=[];try{const raw=JSON.parse(localStorage.getItem(RETRY_MOMENTS_KEY)||"[]");if(Array.isArray(raw))old=raw}catch{};localStorage.setItem(RETRY_MOMENTS_KEY,JSON.stringify([moment,...old].slice(0,40)));window.location.href=`/retry-moment?id=${encodeURIComponent(moment.id)}`}
 return <main className="min-h-screen bg-[#f4f7f7] text-[#172b3a]"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Button asChild variant="ghost"><Link href="/interview-academy"><ArrowLeft/>Interview Academy</Link></Button><Badge variant="outline"><Brain className="size-3.5"/>7 · Interview Replay</Badge></div></header><div className="mx-auto max-w-7xl space-y-6 px-4 py-8"><section><p className="text-xs font-bold uppercase tracking-[.18em] text-[#147d91]">Interview learning evidence</p><h1 className="mt-2 font-serif text-4xl font-bold">Replay the moment the reasoning changed.</h1><p className="mt-3 max-w-3xl text-slate-600">The timeline marks challenges, repairs and changed conditions. Any candidate response can be turned into a fresh “Retry This Moment” exercise, and weaknesses can be scheduled for a delayed retest in a new context.</p></section>{notice&&<div className="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-950">{notice}</div>}{!active?<Card><CardHeader><CardTitle>No live replay yet</CardTitle><CardDescription>Complete a Gemini Live interview. Your transcript, confidence estimates and branch path will appear here.</CardDescription></CardHeader><CardContent><Button asChild><Link href="/gemini-live-interview">Start live interview</Link></Button></CardContent></Card>:<><div className="grid gap-4 lg:grid-cols-[280px_1fr]"><Card><CardHeader><CardTitle className="font-serif text-xl">Saved interviews</CardTitle></CardHeader><CardContent className="space-y-2">{replays.map(item=><button key={item.id} onClick={()=>setActiveId(item.id)} className={`w-full rounded-xl border p-3 text-left ${item.id===active.id?"border-[#147d91] bg-cyan-50":"bg-white"}`}><p className="font-semibold">{item.course}</p><p className="mt-1 text-xs text-slate-500">{new Date(item.date).toLocaleString("en-GB")}</p></button>)}</CardContent></Card><div className="space-y-4"><div className="grid gap-3 md:grid-cols-4"><Card><CardContent className="p-5"><Clock3 className="size-5 text-[#147d91]"/><p className="mt-3 text-3xl font-bold">{Math.round(active.durationSeconds/60)}m</p><p className="text-xs text-slate-500">interview duration</p></CardContent></Card><Card><CardContent className="p-5"><Gauge className="size-5 text-[#147d91]"/><p className="mt-3 text-3xl font-bold">{active.calibration.averageConfidence??"—"}{active.calibration.averageConfidence!==null?"%":""}</p><p className="text-xs text-slate-500">average confidence</p></CardContent></Card><Card><CardContent className="p-5"><TrendingUp className="size-5 text-[#147d91]"/><p className="mt-3 text-3xl font-bold">{active.calibration.lowConfidenceStrongAnswers}</p><p className="text-xs text-slate-500">strong / low confidence</p></CardContent></Card><Card><CardContent className="p-5"><Brain className="size-5 text-[#147d91]"/><p className="mt-3 text-3xl font-bold">{active.calibration.highConfidenceWeakAnswers}</p><p className="text-xs text-slate-500">gaps / high confidence</p></CardContent></Card></div><Card><CardHeader><CardTitle className="font-serif text-2xl">Reasoning timeline</CardTitle><CardDescription>{active.calibration.note} Use Retry on a response you want to rebuild.</CardDescription></CardHeader><CardContent className="space-y-3">{active.turns.filter(turn=>turn.role!=="system").map((turn,index)=>{const sourceIndex=active.turns.indexOf(turn);return <div key={turn.id} className={`rounded-2xl border p-4 ${turn.role==="candidate"?"bg-white":"bg-slate-50"}`}><div className="flex flex-wrap items-center gap-2"><Badge variant={turn.role==="candidate"?"default":"outline"}>{turn.role==="candidate"?"You":`Interviewer ${turn.interviewer??""}`.trim()}</Badge><Badge variant="outline">{turnTime(active,sourceIndex)}</Badge>{turn.role==="interviewer"&&<Badge variant="outline">{branchLabel(turn.branchType)}</Badge>}{typeof turn.confidence==="number"&&<Badge variant="outline">Confidence {turn.confidence}%</Badge>}</div><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{turn.text}</p>{turn.feedback&&<p className="mt-3 rounded-xl bg-amber-50 p-3 text-sm leading-6 text-amber-950"><strong>Feedback:</strong> {turn.feedback}</p>}{turn.workingSummary&&<p className="mt-3 rounded-xl bg-cyan-50 p-3 text-sm leading-6 text-cyan-950"><strong>Working shown:</strong> {turn.workingSummary}</p>}{turn.role==="candidate"&&<Button size="sm" variant="outline" className="mt-3" onClick={()=>retryTurn(sourceIndex)}><RotateCcw/>8 · Retry this moment</Button>}</div>})}</CardContent></Card><div className="grid gap-4 md:grid-cols-2"><Card><CardHeader><CardTitle className="font-serif text-xl">Evidence of strength</CardTitle></CardHeader><CardContent className="space-y-2">{active.strengths.length?active.strengths.map(item=><p key={item} className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-950">{item}</p>):<p className="text-sm text-slate-500">Use the transcript as evidence until more feedback is recorded.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="font-serif text-xl">Priority to improve</CardTitle></CardHeader><CardContent className="space-y-2">{active.improvements.length?active.improvements.map(item=><p key={item} className="rounded-xl bg-amber-50 p-3 text-sm text-amber-950">{item}</p>):<p className="text-sm text-slate-500">No repeated weakness was detected in this session.</p>}</CardContent></Card></div><Card className="border-blue-200 bg-blue-50"><CardHeader><CalendarClock className="size-5 text-blue-700"/><CardTitle className="font-serif text-xl">10 · Delayed retest</CardTitle><CardDescription>{active.focus}</CardDescription></CardHeader><CardContent className="flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-blue-950">Schedule the underlying reasoning demand after a delay. Repeated weakness returns sooner; stable performance is spaced further apart.</p><Button onClick={scheduleRetest}><RotateCcw/>Schedule spaced retest</Button></CardContent></Card><Button asChild variant="outline"><Link href="/interview-profile"><Sparkles/>Open long-term interview profile <ArrowRight/></Link></Button></div></div></>}</div></main>
}
